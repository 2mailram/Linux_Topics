create shell script to reboot remote host - it should do it every sunday morning 5 am
create shell script - if user pass argument as foo it should return bar , if user pass arg bar - should return foo
create infinity loop using while statement
understand difference between break and exit - try to implement in simple if statmeent and for loop
create shell script to replace string in a file using sed or awk
create shell script to clean virtual memory and it should execute at daily morning 5AM
create shell script to restart the httpd service if already running,  if stopped state do only start the application
clean the log if reached more then 5 gb under /var/log/httpd - 
    script should execute only if directory exist 
    script shold execute only httpd service installed 
  
